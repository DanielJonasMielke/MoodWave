# Ignore actual secret files with real credentials
# The moodwave-secrets.yaml template with placeholder values IS committed
# Users should copy it and fill in real values locally

*-real.yaml
*-local.yaml
my-secrets.yaml
credentials.yaml
