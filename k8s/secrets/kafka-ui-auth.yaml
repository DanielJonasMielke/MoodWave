# Kafka UI Authentication Secret
# This secret stores the username and password for Kafka UI access
#
# Default credentials 
#   Username: admin
#   Password: moodwave2026

apiVersion: v1
kind: Secret
metadata:
  name: kafka-ui-auth
  namespace: default
  labels:
    app: moodwave
    component: kafka-ui
type: Opaque
data:
  # Base64 encoded credentials
  # Default: admin / moodwave2026
  username: YWRtaW4=
  password: bW9vZHdhdmUyMDI2
